# Microcontrollers programming

Microcontrollers programming course included three small assignments and one bigger one, that was based on two previous ones.

I've inluded my solutions, as well as the descriptions, to small lab assignments in the `labs/` directory.

The bigger assignment is in the `project-bt1` directory alongside description -- it's purpose was to create a `Game Console` controller using an STM32 microcontroller.